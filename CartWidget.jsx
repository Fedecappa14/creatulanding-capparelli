// src/components/CartWidget.js
import React from 'react';

const CartWidget = () => {
    return (
        <div>
            <button style={{ backgroundColor: '#fff', color: '#333', padding: '5px 10px' }}>Carrito (0)</button>
        </div>
    );
};

export default CartWidget;